/**
 * Mock data toggle.
 *
 * Set to `true` to load sample markets, positions, and stats
 * so you can explore the UI without deployed contracts.
 * See USE_MOCK_DATA.md for details.
 *
 * Default is `false` — hooks return empty state, ready for live RPC.
 */
export const ENABLE_MOCK_DATA = false;
